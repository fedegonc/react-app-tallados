import React from 'react';

const Logo = () => {
  const styles = {
    fontSize: '1.5rem',
    fontWeight: 'bold',
    color: '#ffffff', // Letras blancas
    fontFamily: "'Segoe UI', Tahoma, Geneva, Verdana, sans-serif",
    textTransform: 'uppercase',
    letterSpacing: '2px',
  };

  return <div style={styles}>Tallado Colonial</div>;
};

export default Logo;
