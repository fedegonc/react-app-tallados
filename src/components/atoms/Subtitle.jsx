import React from "react";

const Subtitle = () => {
  const subtitleStyles = {
    fontSize: "1.5rem",
    fontWeight: "normal",
    color: "#6A7B6A",
    margin: "10px 0",
  };

  return <h2 style={subtitleStyles}>Tradición y excelencia en cada detalle</h2>;
};

export default Subtitle;
